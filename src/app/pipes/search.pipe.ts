import { Pipe, PipeTransform } from '@angular/core';import { ArticleInterface } from '../types/article.interface';@Pipe({  name: "search",})export class SearchPipe implements PipeTransform {  transform(article: ArticleInterface[], saerchStrings: string[]): ArticleInterface[] {    if(saerchStrings){      return article.filter(articleName => {        return saerchStrings?.some(substring=>articleName.title.includes(substring))          || saerchStrings?.some(substring=>articleName.summary.includes(substring))      })    }    else { return article }  }}