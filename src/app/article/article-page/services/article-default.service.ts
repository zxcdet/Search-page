import { Injectable } from '@angular/core';import { Observable } from 'rxjs';import { Store } from '@ngrx/store';import { ArticleServiceInterface } from './article-service.interface';import { getArticleSelector } from '../../../store/selectors';import { ArticleInterface } from '../../../types/article.interface';@Injectable()export class ArticleDefaultService implements ArticleServiceInterface{  constructor(private readonly store: Store) {  }  public getArticle(id: string): Observable<ArticleInterface> {    return this.store.select(getArticleSelector(id))  }}