import { createSelector } from '@ngrx/store';import { ArticleStateInterface } from '../types/ArticleState.interface';import { AppStateInterface } from '../types/appState.interface';export const selectFeature = (state: AppStateInterface) => state.auth;export const IsLoadingSelector = createSelector(  selectFeature,  (state: ArticleStateInterface) => state.isLoading)export const getArticlesSelector = createSelector(  selectFeature,  (state: ArticleStateInterface) => state.article)export const getArticleSelector = (id: string) =>  createSelector(    selectFeature,    (state) => state.article.find((article) => article.id.toString() === id)  );